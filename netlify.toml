[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback so client-side routing / direct trace links resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
